<section class="catalog" id="catalog">
    <h2>Book Reviews</h2>
    <div class="container">

        {{#each books}}
        {{> book}}
        {{/each}}

        {{#unless books}}
        <h2 class="no-record">There are no book reviews found yet...</h2>
        {{/unless}}
        
    </div>
</section>